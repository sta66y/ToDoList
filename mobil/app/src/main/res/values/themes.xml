<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Базовая тема с Material3 -->
    <style name="Base.Theme.Mobil" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Основные цвета -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Сбрасываем фон всех кнопок -->
        <item name="materialButtonStyle">@style/MyButtonStyle</item>
    </style>

    <!-- Стиль для всех MaterialButton -->
    <style name="MyButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@null</item>
        <item name="backgroundTint">@null</item>
    </style>

    <!-- Твоя тема приложения -->
    <style name="Theme.Mobil" parent="Base.Theme.Mobil" />

</resources>
